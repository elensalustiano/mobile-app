const tintColorLight = '#2f95dc'

const Colors = {
  text: '#000',
  background: '#fff',
  tint: tintColorLight,
  tabIconDefault: '#ccc',
  activeColor: tintColorLight,
}

const Sizes = {}

export {
  Colors,
  Sizes
}